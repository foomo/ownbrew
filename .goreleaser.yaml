version: 2

project_name: ownbrew

builds:
  - binary: ownbrew
    main: ./main.go
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
      - -tags=safe
    ldflags:
      - -s -w
      - -X github.com/foomo/ownbrew/cmd.version={{.Version}}
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - formats: [ tar.gz ]
    format_overrides:
      - goos: windows
        formats: [ zip ]

release:
  prerelease: auto

changelog:
  use: github-native

brews:
  - repository:
      owner: foomo
      name: homebrew-tap
    caveats: "ownbrew --help"
    homepage: "https://github.com/foomo/ownbrew"
    description: "Your local project package manager"
    test: |
      system "#{bin}/ownbrew version"

dockers:
  - use: buildx
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile
    image_templates:
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:latest-amd64{{ end }}'
      - 'foomo/ownbrew:{{ .Version }}-amd64'
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:{{ .Major }}-amd64{{ end }}'
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:{{ .Major }}.{{ .Minor }}-amd64{{ end }}'
    build_flag_templates:
      - '--pull'
      # https://github.com/opencontainers/image-spec/blob/main/annotations.md#pre-defined-annotation-keys
      - '--label=org.opencontainers.image.title={{.ProjectName}}'
      - '--label=org.opencontainers.image.description=Your local project package manager'
      - '--label=org.opencontainers.image.source={{.GitURL}}'
      - '--label=org.opencontainers.image.url={{.GitURL}}'
      - '--label=org.opencontainers.image.documentation={{.GitURL}}'
      - '--label=org.opencontainers.image.created={{.Date}}'
      - '--label=org.opencontainers.image.revision={{.FullCommit}}'
      - '--label=org.opencontainers.image.version={{.Version}}'
      - '--platform=linux/amd64'

  - use: buildx
    goos: linux
    goarch: arm64
    dockerfile: Dockerfile
    image_templates:
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:latest-arm64{{ end }}'
      - 'foomo/ownbrew:{{ .Version }}-arm64'
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:{{ .Major }}-arm64{{ end }}'
      - '{{ if eq .Prerelease "" }}foomo/ownbrew:{{ .Major }}.{{ .Minor }}-arm64{{ end }}'
    build_flag_templates:
      - '--pull'
      # https://github.com/opencontainers/image-spec/blob/main/annotations.md#pre-defined-annotation-keys
      - '--label=org.opencontainers.image.title={{.ProjectName}}'
      - '--label=org.opencontainers.image.description=Your local project package manager'
      - '--label=org.opencontainers.image.source={{.GitURL}}'
      - '--label=org.opencontainers.image.url={{.GitURL}}'
      - '--label=org.opencontainers.image.documentation={{.GitURL}}'
      - '--label=org.opencontainers.image.created={{.Date}}'
      - '--label=org.opencontainers.image.revision={{.FullCommit}}'
      - '--label=org.opencontainers.image.version={{.Version}}'
      - '--platform=linux/arm64'

docker_manifests:
  # basic
  - name_template: 'foomo/ownbrew:latest'
    image_templates:
      - 'foomo/ownbrew:latest-amd64'
      - 'foomo/ownbrew:latest-arm64'
    skip_push: auto

  - name_template: 'foomo/ownbrew:{{ .Version }}'
    image_templates:
      - 'foomo/ownbrew:{{ .Version }}-amd64'
      - 'foomo/ownbrew:{{ .Version }}-arm64'
    skip_push: auto

  - name_template: 'foomo/ownbrew:{{ .Major }}'
    image_templates:
      - 'foomo/ownbrew:{{ .Major }}-amd64'
      - 'foomo/ownbrew:{{ .Major }}-arm64'
    skip_push: auto

  - name_template: 'foomo/ownbrew:{{ .Major }}.{{ .Minor }}'
    image_templates:
      - 'foomo/ownbrew:{{ .Major }}.{{ .Minor }}-amd64'
      - 'foomo/ownbrew:{{ .Major }}.{{ .Minor }}-arm64'
    skip_push: auto
